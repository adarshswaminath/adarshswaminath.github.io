---
// meta-label.astro technical uppercase label
interface Props {
  class?: string
}
const { class: className = '' } = Astro.props
---

<p
  class={`font-mono text-[10px] tracking-[0.2em] text-(--ink-muted) uppercase ${className}`}
>
  <slot />
</p>
