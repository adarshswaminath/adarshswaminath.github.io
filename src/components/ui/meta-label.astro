---
// meta-label.astro — Docs-style uppercase section label
interface Props {
  class?: string
}
const { class: className = '' } = Astro.props
---

<h4
  class={`font-mono text-[10px] font-semibold tracking-[0.2em] text-[var(--ink-muted)] uppercase ${className}`}
>
  <slot />
</h4>
